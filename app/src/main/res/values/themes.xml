<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme -->
    <style name="Base.Theme.VickyPracticalDevStreeIT" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:statusBarColor">@color/statusBarColor</item>
        <item name="android:navigationBarColor">@color/navigationBarColor</item>
        <item name="toolbarStyle">@style/CustomToolbarStyle</item>
    </style>

    <!-- Custom theme -->
    <style name="Theme.VickyPracticalDevStreeIT" parent="Base.Theme.VickyPracticalDevStreeIT">
        <!-- Customizations specific to this theme -->

    </style>

    <!-- Toolbar style -->
    <style name="CustomToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:titleTextColor">@color/white</item>
        <item name="titleTextAppearance">@style/Toolbar.TitleText</item>
    </style>

    <!-- Toolbar title text style -->
    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">@font/poppins_bold</item>
    </style>

    <!-- Button style -->
    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:padding">10dp</item>
        <item name="android:fontFamily">@font/poppins_bold</item>
    </style>

    <!-- EditText style -->
    <style name="CustomEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/editTextColor</item>
        <item name="android:padding">10dp</item>
    </style>

    <!-- TextView style -->
    <style name="CustomTextViewStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/textViewColor</item>
    </style>

    <!-- ImageButton style -->
    <style name="CustomImageButtonStyle" parent="Widget.AppCompat.ImageButton">
        <item name="android:padding">5dp</item>
    </style>

</resources>
